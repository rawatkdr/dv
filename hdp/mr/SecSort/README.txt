This program simply writes map -> reduce output without modifying anything.
Only a custom partitioner is used.
This version changes the data 
	first aggregates it, then writes rank over as output.
	second change
